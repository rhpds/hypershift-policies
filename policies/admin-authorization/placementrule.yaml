---
apiVersion: apps.open-cluster-management.io/v1
kind: PlacementRule
metadata:
  name: admin-authorization
  namespace: rhdp-policies
spec:
  clusterSelector:
    matchLabels:
      vendor: OpenShift
      type: sandbox
